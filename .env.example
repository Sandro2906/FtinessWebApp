# Database Configuration
# If you do not set any DATABASE_URL, Prisma falls back to a SQLite file (./dev.db).
# To use PostgreSQL (or another provider), specify:
#   DATABASE_PROVIDER="postgresql"
#   DATABASE_URL="postgresql://user:pass@host:port/database"
# Example for local Docker Postgres:
#   DATABASE_PROVIDER="postgresql"
#   DATABASE_URL="postgresql://testuser:testpassword@localhost:5432/nextjs_db"
DATABASE_URL=""

# PayPal Configuration
# Get from: https://developer.paypal.com/
NEXT_PUBLIC_PAYPAL_CLIENT_ID="your_paypal_client_id_here"

# Email Configuration (SMTP)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your_email@gmail.com"
SMTP_PASS="your_app_password"

# Node Environment
NODE_ENV="development"
