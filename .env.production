# Production Environment - Set these on Vercel dashboard
# DATABASE_URL is required and should be set in Vercel project settings
# Format: postgresql://user:password@host:port/database

# PayPal (production)
NEXT_PUBLIC_PAYPAL_CLIENT_ID=""

# Email (SMTP)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
